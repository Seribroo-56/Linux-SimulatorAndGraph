README.md
## Running TypeScript Code

This project uses **TypeScript**, a strongly typed superset of JavaScript. Follow the steps below to compile and run the code.

---

### 1. Install Node.js and npm

Make sure you have **Node.js** and **npm** installed on your system:
run with **npm run dev**
```bash
node -v
npm -v
